---
phase: 02-agents-and-game
plan: 06
type: execute
wave: 4
depends_on: ["02-04", "02-05"]
files_modified:
  - agents/red_agent/Dockerfile
  - agents/blue_agent/Dockerfile
  - docker-compose.yml
  - agents/requirements.txt
  - tests/test_skills.py
  - tests/test_game.py
autonomous: false

must_haves:
  truths:
    - "docker compose up builds all containers without errors"
    - "Red agent completes a 4-step kill chain autonomously"
    - "Blue agent hardens during setup and detects/responds during battle"
    - "A full game runs to a scored conclusion with narrative output"
    - "At least one stealth or detection bonus is recorded"
  artifacts:
    - path: "tests/test_skills.py"
      provides: "Unit tests for skill registry and skill functions"
    - path: "tests/test_game.py"
      provides: "Unit tests for game state, scoring, and narrative"
  key_links:
    - from: "docker-compose.yml"
      to: "game/loop.py"
      via: "game loop calls orchestrator HTTP endpoints inside Docker network"
      pattern: "orchestrator.*8000"
---

<objective>
Integration testing: update Docker configs, run unit tests, and verify a complete game runs end-to-end.

Purpose: All components are built. This plan wires them together in Docker, verifies the build, runs unit tests on the host, and then executes a full game to validate all 5 success criteria. This is the final validation before Phase 2 is marked complete.

Output: Working Docker setup, passing tests, and a verified full game run.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-agents-and-game/02-04-SUMMARY.md
@.planning/phases/02-agents-and-game/02-05-SUMMARY.md
@docker-compose.yml
@agents/red_agent/Dockerfile
@agents/blue_agent/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker config updates, dependency wiring, and unit tests</name>
  <files>docker-compose.yml, agents/red_agent/Dockerfile, agents/blue_agent/Dockerfile, agents/requirements.txt, tests/test_skills.py, tests/test_game.py</files>
  <action>
**docker-compose.yml updates:**
- Add `skills/` directory as build context for agent containers. Agent containers need the skills package because base_agent.py imports `from skills.registry import get_skills_for_role` to build the skill metadata list for agent prompts.

**agents/red_agent/Dockerfile and agents/blue_agent/Dockerfile:**
- Add `COPY skills/ /app/skills/` so the skills package is available inside agent containers. This is required because base_agent.py imports skills.registry for prompt construction.
- Ensure langgraph, requests are in requirements.txt and installed.

**agents/requirements.txt:**
- Ensure langgraph, requests are listed. Remove python-nmap if present (skills are server-side on battleground).

**tests/test_skills.py:**
- Test skill registry: import SKILL_REGISTRY, verify 18 skills registered (8 red + 10 blue)
- Test get_skills_for_role returns correct subset
- Test each skill function signature (accepts orchestrator_url, returns dict) — use mock for requests.post
- Test _parse_skill_call with valid JSON, mixed text + JSON, and garbage input

**tests/test_game.py:**
- Test GamePhase transitions (SETUP -> BATTLE -> CONCLUSION)
- Test check_win_condition for each of 4 conditions
- Test ScoringEngine.award returns correct points
- Test ScoringEngine.check_kill_chain_progress
- Test generate_narrative produces non-empty string
- All tests use mocks (no Docker required)

Run: `python -m pytest tests/test_skills.py tests/test_game.py -v`
  </action>
  <verify>
`python -m pytest tests/test_skills.py tests/test_game.py -v` — all tests pass.
`docker compose build` completes without errors.
`grep "COPY skills" agents/red_agent/Dockerfile` confirms skills directory is copied.
  </verify>
  <done>All unit tests pass, Docker builds succeed, skill and game modules are properly wired. Agent containers include skills/ package via COPY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 2: agent skill system, game engine, game loop, battleground vulnerabilities, and Docker integration. The system should be capable of running a full autonomous cybersecurity game.</what-built>
  <how-to-verify>
1. Run `docker compose build` — all images build without errors.
2. Run `docker compose up -d` — all containers start (check `docker compose ps`).
3. Verify orchestrator health: `curl http://localhost:8000/health` returns `{"status": "ok"}`.
4. Verify game endpoints exist: `curl http://localhost:8000/docs` — check /game/start, /game/status, /game/score, /game/narrative are listed.
5. Start a game: `curl -X POST http://localhost:8000/game/start` — returns status "started".
6. Check game status: `curl http://localhost:8000/game/status` — returns phase, scores, win_condition.
7. If KoboldCpp is running: execute `python -m game.loop` for a short test game (reduce setup to 1 min, battle to 3 min). Verify:
   - Blue agent executes hardening actions during setup
   - Blue setup actions are scored (check /game/status shows blue points > 0 after setup)
   - Both agents alternate turns during battle
   - Game terminates with a scored conclusion
   - Narrative output tells the game story
8. If KoboldCpp is NOT available: verify with mock by checking that the game loop logic runs without crashes using the orchestrator's existing test infrastructure.
  </how-to-verify>
  <resume-signal>Type "approved" if a game runs to completion, or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
- All unit tests pass (pytest)
- Docker compose builds and starts all containers
- Orchestrator serves game control endpoints
- A full game produces scored output with narrative
</verification>

<success_criteria>
Phase 2 success criteria (all 5):
1. Red agent completes 4-step kill chain autonomously with decision log reasoning
2. Blue agent hardens during setup (firewall, SSH, detection active) before battle — setup actions scored
3. Full game runs setup -> battle (5+ min) -> conclusion via win condition
4. Scoring engine produces final summary (red pts, blue pts, win condition) readable to non-technical observer
5. At least one stealth or detection bonus recorded in test game
</success_criteria>

<output>
After completion, create `.planning/phases/02-agents-and-game/02-06-SUMMARY.md`
</output>
