---
phase: 03-demo-ready
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - display/dashboard.py
  - display/components.py
  - display/__init__.py
autonomous: true

must_haves:
  truths:
    - "Terminal shows red agent actions in a red-bordered panel on the left"
    - "Terminal shows blue agent actions in a blue-bordered panel on the right"
    - "Terminal shows live scoreboard in the center with both scores and turn count"
    - "Display updates automatically every 1-2 seconds during a game"
    - "Agent status shows thinking/executing/idle indicator to avoid dead air"
  artifacts:
    - path: "display/components.py"
      provides: "Reusable Rich renderables: scoreboard table, agent panel, header, phase indicator"
      exports: ["build_scoreboard", "build_agent_panel", "build_header", "build_phase_indicator"]
    - path: "display/dashboard.py"
      provides: "Rich Live dashboard that polls orchestrator API"
      exports: ["run_dashboard"]
  key_links:
    - from: "display/dashboard.py"
      to: "http://localhost:8000/game/status"
      via: "httpx or requests GET polling"
      pattern: "requests\\.get.*game/status"
    - from: "display/dashboard.py"
      to: "http://localhost:8000/decisions/"
      via: "httpx or requests GET polling"
      pattern: "requests\\.get.*decisions"
---

<objective>
Build the Rich terminal dashboard that shows real-time game progress to a non-technical audience.

Purpose: PRES-01 — the demo's primary visual interface. Must be legible, colorful, and informative.
Output: `display/dashboard.py` and `display/components.py` ready to run with `python -m display.dashboard`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-demo-ready/03-RESEARCH.md
@orchestrator/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rich display components</name>
  <files>display/__init__.py, display/components.py</files>
  <action>
Create `display/__init__.py` (empty).

Create `display/components.py` with these functions:

1. `build_header(phase: str, game_id: int) -> Panel` — centered title "CYBERSEC AI CAPSTONE - LIVE GAME" with phase name and game ID, white on dark background.

2. `build_scoreboard(red_score: int, blue_score: int, turn: int, max_turns: int, phase: str) -> Table` — Rich Table titled "SCOREBOARD" with columns Team and Score. Rows: Red Team (red styled), Blue Team (blue styled), Turn (N/M), Phase. Use `show_header=True`.

3. `build_agent_panel(role: str, actions: list[dict], status: str = "idle") -> Panel` — Panel with border_style matching role color (red/blue). Shows last 5 actions from the actions list (each with command and truncated stdout at 200 chars). At top, show status with a spinner character when status is "thinking" or "executing". Title: "RED AGENT" or "BLUE AGENT".

4. `build_phase_indicator(phase: str) -> Text` — Rich Text showing current game phase with color coding (setup=yellow, battle=red, conclusion=green).

5. `build_footer() -> Panel` — Shows "Press Ctrl+C to exit" centered.

All functions return Rich renderables. No I/O, no API calls — pure rendering functions.
  </action>
  <verify>python -c "from display.components import build_scoreboard, build_agent_panel, build_header, build_phase_indicator, build_footer; print('OK')"</verify>
  <done>All 5 component functions importable and return Rich renderables when called with sample data.</done>
</task>

<task type="auto">
  <name>Task 2: Live dashboard with API polling</name>
  <files>display/dashboard.py</files>
  <action>
Create `display/dashboard.py` with:

1. `make_layout() -> Layout` — Split into header (size=3), body, footer (size=3). Body splits into three columns: red (ratio=1), scores (ratio=1), blue (ratio=1).

2. `fetch_game_status(base_url: str) -> dict` — GET `{base_url}/game/status`, return JSON. On connection error, return a fallback dict with phase="unknown", scores=0.

3. `fetch_agent_actions(base_url: str, role: str, limit: int = 5) -> list[dict]` — GET `{base_url}/decisions/{role}`, return list. On error, return empty list.

4. `run_dashboard(base_url: str = "http://localhost:8000", refresh_rate: float = 1.0)` — Main loop:
   - Create layout via `make_layout()`
   - Use `Live(layout, refresh_per_second=1, screen=True)` context
   - In loop: fetch game status and both agents' actions via the API functions
   - Update layout panels using components from components.py
   - Determine agent status from game phase and action timestamps (if last action < 5s ago -> "executing", elif game active -> "thinking", else "idle")
   - Sleep `refresh_rate` seconds between polls
   - Catch KeyboardInterrupt to exit cleanly

5. `if __name__ == "__main__":` block with argparse: `--url` (default localhost:8000), `--refresh` (default 1.0).

Use `requests` library for HTTP (add to requirements if needed). Do NOT use print() — only `live.console.log()` if logging is needed. Use `screen=True` for proper alternate screen buffer.
  </action>
  <verify>python -c "from display.dashboard import make_layout, fetch_game_status; print(make_layout()); print('OK')"</verify>
  <done>Dashboard module importable. `make_layout()` returns a 3-column Layout. Running `python -m display.dashboard` shows the dashboard UI (exits cleanly on Ctrl+C even without a running game).</done>
</task>

</tasks>

<verification>
1. `python -c "from display.components import build_scoreboard; print(build_scoreboard(10, 5, 3, 20, 'battle'))"` prints a formatted table
2. `python -m display.dashboard --url http://localhost:1 --refresh 0.1` starts, shows fallback "unknown" state, exits on Ctrl+C without traceback
</verification>

<success_criteria>
- display/components.py exports 5 rendering functions
- display/dashboard.py polls orchestrator API and renders a 3-column Rich Live layout
- Dashboard handles connection errors gracefully (shows fallback state, doesn't crash)
- No print() statements — all output via Rich
</success_criteria>

<output>
After completion, create `.planning/phases/03-demo-ready/03-01-SUMMARY.md`
</output>
