---
phase: 03-demo-ready
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - display/replay.py
autonomous: true

must_haves:
  truths:
    - "A completed game's decision log can be replayed in the terminal with simulated timing"
    - "Replay uses the same Rich components as the live dashboard for visual consistency"
    - "Replay speed is adjustable via CLI flag"
  artifacts:
    - path: "display/replay.py"
      provides: "CLI replay viewer reading from orchestrator API or SQLite"
      exports: ["replay_game"]
  key_links:
    - from: "display/replay.py"
      to: "display/components.py"
      via: "import build_scoreboard, build_agent_panel"
      pattern: "from display\\.components import"
    - from: "display/replay.py"
      to: "orchestrator API or SQLite"
      via: "fetches all decisions for a game"
      pattern: "decisions|decision_log"
---

<objective>
Build a post-game replay viewer that renders a completed game's decision log with simulated timing.

Purpose: PRES-03 — lets the presenter walk through a completed game at adjustable speed.
Output: `display/replay.py` runnable as `python -m display.replay`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-demo-ready/03-RESEARCH.md
@.planning/phases/03-demo-ready/03-01-SUMMARY.md
@orchestrator/main.py
@orchestrator/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replay viewer CLI</name>
  <files>display/replay.py</files>
  <action>
Create `display/replay.py`:

1. `fetch_all_decisions(base_url: str) -> list[dict]` — GET `/decisions/red` and `/decisions/blue`, merge and sort by timestamp (or by id). Return combined list.

2. `replay_game(base_url: str = "http://localhost:8000", speed: float = 1.0)`:
   - Fetch all decisions via `fetch_all_decisions()`
   - If no decisions, print "No game data found" and exit
   - Create layout using `make_layout()` from dashboard.py
   - Use `Live(layout, screen=True)` context
   - Iterate through decisions in chronological order
   - For each decision:
     - Calculate delay from previous decision timestamp, divide by speed multiplier
     - `time.sleep(delay)` (min 0.1s, max 3.0s to keep it watchable)
     - Update the appropriate agent panel (red or blue) with this action
     - Update scoreboard if score data available from `/game/status`
   - After all decisions rendered, show "REPLAY COMPLETE" in header and wait for Ctrl+C

3. `if __name__ == "__main__":` with argparse:
   - `--url` (default http://localhost:8000)
   - `--speed` (default 1.0, e.g. 2.0 for 2x speed, 0.5 for half speed)

Reuse components from `display.components` (build_agent_panel, build_scoreboard, build_header).
Reuse `make_layout` from `display.dashboard`.
Use requests for HTTP calls. No print() — only Rich output.
  </action>
  <verify>python -c "from display.replay import fetch_all_decisions, replay_game; print('OK')"</verify>
  <done>replay.py importable. Running `python -m display.replay --url http://localhost:1` shows "No game data found" or connection error gracefully.</done>
</task>

</tasks>

<verification>
1. `python -c "from display.replay import replay_game; print('OK')"` — importable
2. Module handles missing/unreachable server gracefully (no traceback)
</verification>

<success_criteria>
- Replay viewer reads decision log via API and renders with simulated timing
- Same Rich components as live dashboard (visual consistency)
- Speed adjustable via --speed flag
- Handles empty game data and connection errors gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/03-demo-ready/03-03-SUMMARY.md`
</output>
