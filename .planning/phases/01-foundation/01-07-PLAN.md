---
phase: 01-foundation
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: [agents/base_agent.py, tests/test_act_node_parsing.py]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Agent correctly reads exit_code from nested result dict after orchestrator allows a command"
    - "Agent detects blocked commands via allowed=False, not a nonexistent blocked key"
    - "Findings accumulate when commands succeed (exit_code 0 with output)"
    - "Observations contain real stdout, not empty strings"
  artifacts:
    - path: "agents/base_agent.py"
      provides: "Corrected act_node response parsing"
      contains: "cmd_result = result.get"
    - path: "tests/test_act_node_parsing.py"
      provides: "Regression test for response parsing"
  key_links:
    - from: "agents/base_agent.py"
      to: "orchestrator ExecuteResponse"
      via: "result.get('result') nested dict"
      pattern: 'cmd_result\.get\("exit_code"'
---

<objective>
Fix the structural wiring bug in act_node response parsing so agents correctly interpret orchestrator responses.

Purpose: Without this fix, agents always see exit_code=-1, empty stdout, and was_blocked=False for every command — making findings and observations useless.
Output: Corrected parsing code + regression test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@agents/base_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix act_node response parsing</name>
  <files>agents/base_agent.py</files>
  <action>
In `act_node` function (around lines 276-279), replace the three incorrect parsing lines:

```python
exit_code = result.get("exit_code", -1)
stdout = result.get("stdout", "")
was_blocked = result.get("blocked", False)
```

With correct parsing that matches the orchestrator's ExecuteResponse schema (`{allowed, reason, blocked_command, result: {stdout, stderr, exit_code, ...}}`):

```python
was_blocked = not result.get("allowed", True)
cmd_result = result.get("result") or {}
exit_code = cmd_result.get("exit_code", -1)
stdout = cmd_result.get("stdout", "")
```

Do NOT change any other code in the function. The downstream usage of exit_code, stdout, and was_blocked remains the same.
  </action>
  <verify>Run `python -c "import agents.base_agent"` to confirm no syntax errors. Grep the file for `cmd_result = result.get` to confirm the fix is in place.</verify>
  <done>Lines 276-279 use nested parsing: was_blocked derived from allowed, exit_code and stdout from result.result.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression test for act_node parsing</name>
  <files>tests/test_act_node_parsing.py</files>
  <action>
Create a test file that validates the parsing logic by mocking the HTTP call to the orchestrator. Test three scenarios:

1. **Allowed command with output**: Mock `requests.post` to return `{"allowed": True, "reason": "ok", "result": {"exit_code": 0, "stdout": "flag found", "stderr": ""}}`. Assert exit_code==0, stdout contains "flag", was_blocked==False, and a finding is appended.

2. **Blocked command**: Mock return `{"allowed": False, "reason": "dangerous", "blocked_command": "rm -rf /", "result": null}`. Assert was_blocked==True, exit_code==-1, stdout=="".

3. **Allowed command with nonzero exit**: Mock return `{"allowed": True, "reason": "ok", "result": {"exit_code": 1, "stdout": "error msg", "stderr": "fail"}}`. Assert exit_code==1, stdout contains "error", was_blocked==False, no finding appended (exit_code != 0).

Use `unittest.mock.patch` on `requests.post`. Import `act_node` from `agents.base_agent`. Construct a minimal state dict matching what act_node expects (with keys: findings, observations, executed_commands, etc.).
  </action>
  <verify>Run `pytest tests/test_act_node_parsing.py -v` — all 3 tests pass.</verify>
  <done>Three regression tests pass, covering allowed/blocked/error response parsing.</done>
</task>

</tasks>

<verification>
- `grep -n "cmd_result" agents/base_agent.py` shows the nested parsing pattern
- `grep -n "was_blocked = not result" agents/base_agent.py` shows correct blocked detection
- `pytest tests/test_act_node_parsing.py -v` passes all tests
</verification>

<success_criteria>
- act_node correctly parses exit_code, stdout from nested result dict
- was_blocked derived from allowed field (not nonexistent blocked field)
- Findings accumulate for successful commands (exit_code 0 with output)
- Three regression tests confirm parsing for allowed, blocked, and error cases
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-07-SUMMARY.md`
</output>
