---
phase: 01-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/red_agent/Dockerfile
  - agents/blue_agent/Dockerfile
  - agents/red_agent/main.py
  - agents/blue_agent/main.py
  - .env
  - .env.example
  - docker-compose.yml
  - orchestrator/ssh_executor.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Red agent container builds and runs agent.py with LangGraph dependencies installed"
    - "Blue agent container builds and runs agent.py with LangGraph dependencies installed"
    - "No hardcoded passwords in source — all secrets from .env file"
  artifacts:
    - path: "agents/red_agent/Dockerfile"
      provides: "Copies agent.py, base_agent.py, requirements.txt; installs deps; runs agent.py"
      contains: "COPY.*agent.py"
    - path: "agents/blue_agent/Dockerfile"
      provides: "Same as red agent Dockerfile pattern"
      contains: "COPY.*agent.py"
    - path: ".env"
      provides: "Environment secrets"
      contains: "BATTLEGROUND_PASSWORD"
  key_links:
    - from: "agents/red_agent/Dockerfile"
      to: "agents/base_agent.py"
      via: "COPY from parent directory"
      pattern: "COPY.*base_agent"
    - from: "docker-compose.yml"
      to: ".env"
      via: "env_file or variable interpolation"
      pattern: "\\$\\{BATTLEGROUND_PASSWORD\\}"
---

<objective>
Fix broken agent Dockerfiles so containers actually run the LangGraph agent code, and extract hardcoded passwords into a .env file.

Purpose: Without this, agent containers just sleep forever and never exercise the orchestrator. Hardcoded passwords are a security anti-pattern flagged in verification.
Output: Working Dockerfiles, .env file, updated docker-compose.yml and ssh_executor.py
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/base_agent.py
@agents/red_agent/agent.py
@agents/blue_agent/agent.py
@agents/requirements.txt
@docker-compose.yml
@orchestrator/ssh_executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix agent Dockerfiles and entry points</name>
  <files>
    agents/red_agent/Dockerfile
    agents/blue_agent/Dockerfile
    agents/red_agent/main.py
    agents/blue_agent/main.py
  </files>
  <action>
    Update both agent Dockerfiles to:
    1. COPY ../requirements.txt and pip install -r requirements.txt (langgraph, langchain-core, requests, pydantic)
    2. COPY ../base_agent.py into /app/
    3. COPY agent.py into /app/
    4. COPY main.py into /app/
    5. CMD ["python", "agent.py"]

    IMPORTANT: Docker COPY cannot reference parent directories with `../`. The build context in docker-compose.yml is `./agents/red_agent`. Two options:
    - Option A (preferred): Change docker-compose.yml build context to `./agents` and set dockerfile to `red_agent/Dockerfile`. Then COPY paths work naturally.
    - Option B: Keep context as-is but update main.py to be the real entry point that imports and runs agent.py logic.

    Choose Option A. Update docker-compose.yml build contexts:
    - red-agent: context: ./agents, dockerfile: red_agent/Dockerfile
    - blue-agent: context: ./agents, dockerfile: blue_agent/Dockerfile

    Then in each Dockerfile:
    ```
    FROM python:3.11-slim
    RUN apt-get update && apt-get install -y --no-install-recommends \
        <existing tools> \
        && rm -rf /var/lib/apt/lists/*
    WORKDIR /app
    COPY requirements.txt .
    RUN pip install --no-cache-dir -r requirements.txt
    COPY base_agent.py .
    COPY red_agent/agent.py .
    COPY red_agent/main.py .
    CMD ["python", "agent.py"]
    ```

    Keep red_agent's existing apt packages (nmap netcat-openbsd iputils-ping).
    Keep blue_agent's existing apt packages (auditd net-tools iputils-ping).

    Replace main.py in both agents with a simple wrapper that imports and calls the agent:
    ```python
    """Entry point — delegates to agent.py."""
    from agent import main
    if __name__ == "__main__":
        main()
    ```
    (Keep as fallback, CMD still runs agent.py directly.)
  </action>
  <verify>
    Run `docker compose build red-agent blue-agent` from project root. Both must build without errors. Verify with:
    `docker compose run --rm red-agent python -c "from base_agent import run_agent; print('OK')"`
    `docker compose run --rm blue-agent python -c "from base_agent import run_agent; print('OK')"`
  </verify>
  <done>Both agent containers build successfully and can import base_agent.run_agent</done>
</task>

<task type="auto">
  <name>Task 2: Extract hardcoded passwords to .env</name>
  <files>
    .env
    .env.example
    .gitignore
    docker-compose.yml
    orchestrator/ssh_executor.py
  </files>
  <action>
    1. Create `.env` file:
       ```
       BATTLEGROUND_PASSWORD=Pr0jectAdmin1
       ```

    2. Create `.env.example` file (safe to commit):
       ```
       BATTLEGROUND_PASSWORD=changeme
       ```

    3. Add `.env` to `.gitignore` if not already present.

    4. Update `docker-compose.yml` line 43 — change:
       `- BATTLEGROUND_PASSWORD=Pr0jectAdmin1`
       to:
       `- BATTLEGROUND_PASSWORD=${BATTLEGROUND_PASSWORD}`

    5. Update `orchestrator/ssh_executor.py` line 50 — change the hardcoded fallback:
       `password = os.environ.get("BATTLEGROUND_PASSWORD", "Pr0jectAdmin1")`
       to:
       `password = os.environ.get("BATTLEGROUND_PASSWORD")`
       And add a check right after: if password is None, raise ValueError("BATTLEGROUND_PASSWORD environment variable is required").
  </action>
  <verify>
    Run `grep -r "Pr0jectAdmin1" . --include="*.py" --include="*.yml" --include="*.yaml"` — must return zero results.
    Run `grep "BATTLEGROUND_PASSWORD" .env` — must show the value.
    Run `grep ".env" .gitignore` — must show .env is ignored.
  </verify>
  <done>No hardcoded passwords in source files. All secrets loaded from .env via environment variables.</done>
</task>

</tasks>

<verification>
- `docker compose build` completes without errors for all services
- `docker compose run --rm red-agent python -c "from base_agent import run_agent; print('OK')"` prints OK
- `docker compose run --rm blue-agent python -c "from base_agent import run_agent; print('OK')"` prints OK
- `grep -r "Pr0jectAdmin1" . --include="*.py" --include="*.yml"` returns nothing
- `.env` file exists with BATTLEGROUND_PASSWORD set
</verification>

<success_criteria>
Agent containers build with all dependencies, can import and run the LangGraph agent code, and no hardcoded secrets remain in source files.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
